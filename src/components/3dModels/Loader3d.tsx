import { Html } from '@react-three/drei'
import { Loader } from '../'

const Loader3d = () => {
	return (
		<Html center>
			<Loader title="Loading Model..." />
		</Html>
	)
}

export default Loader3d
